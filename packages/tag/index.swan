<filter src="../_filter/utils.filter.js" module="utils" />

<view
  class="custom-class {{ utils.bem('tag', [size, { mark, plain, round }]) }} {{ utils.judge(plain , 'van-hairline--surround' , '') }}"
  style="{{ style }}"
>
  <slot />
</view>
